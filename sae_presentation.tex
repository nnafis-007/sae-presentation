\documentclass[aspectratio=169]{beamer}

\usetheme{metropolis}
\setbeamertemplate{navigation symbols}{}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{array}
\graphicspath{{images/}}

\title{Sparse Autoencoders (SAE) for LLM Interpretability}
\subtitle{From superposition to monosemantic features}
\author{330 ML Lab}
\date{}

\newcommand{\slideimg}[1]{%
  \begin{center}
    \includegraphics[width=0.88\textwidth,height=0.58\textheight,keepaspectratio]{#1}
  \end{center}
}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Journey to SAE-based Model Interpretation}
\begin{enumerate}
  \item Understanding the superposition problem in LLMs
  \item Collecting intuition on how SAE can help
  \item SAE technical framework from recent survey literature
  \item Architectural shift from vanilla SAE to TopK/Batch-TopK/FC variants
  \item Training workflow and health checks
  \item Evaluation: reconstruction, sparsity, and feature behavior
  \item Interpretation of learned feature families and limitations
  \item Inference UI walkthrough for practical feature analysis
\end{enumerate}
\end{frame}

\begin{frame}{1) Superposition in LLMs}
\textbf{Core issue}
\begin{itemize}
  \item LLM activations can encode more independent features than available neurons
  \item A single neuron may respond to multiple unrelated concepts
  \item This appears as \textbf{polysemanticity}, a signature of superposition
\end{itemize}

\vspace{0.4em}
\textbf{Consequence}
\begin{itemize}
  \item Neuron-level interpretation becomes unstable and ambiguous
  \item Hard to assign one clean concept to one neuron
\end{itemize}
\end{frame}

\begin{frame}{Superposition Intuition}
\begin{columns}[T,onlytextwidth]
\column{0.55\textwidth}
\textbf{Geometric view}
\begin{itemize}
  \item Latent space has dimension $d$
  \item Model needs to represent $m$ concepts with $m \gg d$
  \item Concepts are packed as overlapping linear combinations
\end{itemize}

Observed activation vector:
\[
z \approx \sum_{i=1}^{m} a_i f_i
\]
\begin{itemize}
  \item $f_i$: feature directions
  \item $a_i$: feature strengths
\end{itemize}

\column{0.43\textwidth}
\includegraphics[width=\linewidth,height=0.62\textheight,keepaspectratio]{Usual_LLM_superposition.png}
\end{columns}
\end{frame}

\begin{frame}{Why This Hurts Interpretability}
\begin{itemize}
  \item Raw neuron activations are not aligned with human concepts
  \item A neuron can fire for different causes in different contexts
  \item Causal interventions at neuron level can be noisy
\end{itemize}

\vspace{0.5em}
\textbf{Goal of SAE-based MI}
\begin{itemize}
  \item Decompose dense/polysemantic representations
  \item Recover sparse, more monosemantic feature activations
\end{itemize}
\end{frame}

\begin{frame}{2) How Sparsity Solves Superposition}
\textbf{Key idea: sparse dictionary coding}

Learn:
\begin{itemize}
  \item an overcomplete dictionary ($m > d$)
  \item sparse coefficients so only a few features are active per token
\end{itemize}

Then each input is reconstructed by a small active subset:
\[
z \rightarrow h(z) \rightarrow \hat{z}
\]

\begin{itemize}
  \item Sparsity encourages feature specialization
  \item Specialization improves interpretability and causal tracing
\end{itemize}
\end{frame}

\begin{frame}{Sparse Representation Principle}
Given encoder output $h(z)$:
\begin{itemize}
  \item Most entries in $h(z)$ should be 0 (or near 0)
  \item Only informative features should activate
\end{itemize}

This pushes the model away from:
\begin{itemize}
  \item ``everything somewhat active''
\end{itemize}

Toward:
\begin{itemize}
  \item ``few meaningful features strongly active''
\end{itemize}

Result: cleaner feature-to-concept mapping.
\end{frame}

\begin{frame}{3) SAE Intro: Architecture}
\begin{columns}[T,onlytextwidth]
\column{0.54\textwidth}
Standard SAE components:
\begin{enumerate}
  \item \textbf{Encoder}: $z \in \mathbb{R}^d \rightarrow h(z) \in \mathbb{R}^m$
  \item \textbf{Decoder}: reconstruct $\hat{z}$ from sparse code
\end{enumerate}

Typical form:
\[
\hat{z} = h(z) W_{dec} + b_{dec}
\]

\begin{itemize}
  \item Overcomplete regime: $m \gg d$
  \item Decoder columns represent candidate interpretable features
\end{itemize}

\column{0.44\textwidth}
\includegraphics[width=\linewidth,height=0.62\textheight,keepaspectratio]{SAE_architecture.png}
\end{columns}
\end{frame}

\begin{frame}{SAE Loss (L1-Regularized Form)}
From the survey's standard objective:
\[
L(z) = \|z - \hat{z}\|_2^2 + \alpha \|h(z)\|_1
\]

\begin{itemize}
  \item Reconstruction term keeps information fidelity
  \item $L_1$ term enforces sparse activations
  \item $\alpha$ controls trade-off
\end{itemize}

\textbf{Trade-off}
\begin{itemize}
  \item High $\alpha$: more sparse, possibly worse reconstruction
  \item Low $\alpha$: better reconstruction, less disentanglement
\end{itemize}
\end{frame}

\begin{frame}{SAE Loss (KL Sparsity Form)}
From Andrew Ng sparse AE notes:
\[
J_{sparse}(W,b) = J(W,b) + \beta \sum_{j=1}^{s_2} KL(\rho\,\|\,\hat{\rho}_j)
\]

Where:
\begin{itemize}
  \item $\rho$: target average activation (small, e.g., 0.05)
  \item $\hat{\rho}_j$: empirical average activation of hidden unit $j$
  \item $\beta$: sparsity penalty weight
\end{itemize}

Interpretation:
\begin{itemize}
  \item Explicitly forces each hidden unit to be rarely active on average
  \item KL term is minimized when $\hat{\rho}_j = \rho$ and grows rapidly near 0/1
\end{itemize}
\end{frame}

\begin{frame}{From Survey Taxonomy to Practice}
The survey groups SAE evolution into two broad directions:
\begin{itemize}
  \item \textbf{Architecture improvements}: Gated, TopK, Batch TopK, JumpReLU, Switch-style routing
  \item \textbf{Training/evaluation improvements}: better sparsity control, dead-feature mitigation, stronger diagnostics
\end{itemize}

\begin{block}{Practical takeaway}
Do not optimize only reconstruction loss; monitor sparsity quality and feature utilization jointly.
\end{block}
\end{frame}

\begin{frame}{Tuning Parameters (Practical)}
\textbf{Core knobs}
\begin{itemize}
  \item Dictionary size $m$ (expansion factor)
  \item Sparsity strength: $\alpha$ (L1) or $(\rho, \beta)$ (KL)
  \item Learning rate and optimizer settings
  \item Batch size and token sampling strategy
\end{itemize}

\textbf{Typical tuning workflow}
\begin{enumerate}
  \item Start with stable reconstruction
  \item Increase sparsity gradually
  \item Track dead features and feature usage spread
  \item Select checkpoint with best interpretability/reconstruction balance
\end{enumerate}
\end{frame}

\begin{frame}{TopK SAE and Batch TopK Intuition}
TopK SAE enforces sparsity by selection, not by $L_1$ penalty.

Encoder idea:
\[
\tilde{h}(z) = TopK\big(W_{enc}(z - b_{pre})\big)
\]

Training objective (survey form):
\[
L(z) = \|z - \hat{z}\|_2^2 + \alpha L_{aux}
\]

\begin{itemize}
  \item Keep only top-$K$ activations per token
  \item Avoids direct magnitude shrinkage from L1
  \item Batch TopK variants stabilize activation budget across a batch
  \item Auxiliary terms help reduce dead-latent collapse
\end{itemize}
\end{frame}

\begin{frame}{FC\_SAE (Feature Choice SAE)}
Feature Choice SAE changes sparsity allocation direction.

Instead of:
\begin{itemize}
  \item fixed active features per token
\end{itemize}

It enforces:
\begin{itemize}
  \item each feature gets assigned to a controlled number of tokens
\end{itemize}

Survey statement (Ayonrinde, 2024):
\[
\sum_j S_{i,j} = m,\ \forall i
\]

\begin{itemize}
  \item $S_{i,j}$ indicates whether feature $i$ is active for token $j$
  \item Promotes more uniform feature utilization
  \item Targets dead-feature collapse directly
\end{itemize}
\end{frame}

\begin{frame}{Architectures at a Glance}
\slideimg{types_of_sae.png}
\begin{itemize}
  \item Vanilla SAE: simple baseline with soft sparsity penalties
  \item TopK/Batch-TopK: explicit activation budget per token/batch
  \item FC-style assignment: balances utilization pressure across features
\end{itemize}
\end{frame}

\begin{frame}{4) Architectural Shift: Why Move Beyond Vanilla SAE?}
Reported issues in standard SAE variants:
\begin{itemize}
  \item Shrinkage bias from $L_1$ penalties
  \item Dead latents (features that never activate)
  \item Rigid sparsity allocation
\end{itemize}

New architectures aim to keep:
\begin{itemize}
  \item strong reconstruction
  \item sparse features
  \item healthier feature utilization
\end{itemize}
\end{frame}

\begin{frame}{Architectural Comparison (Quick)}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{0.2\textwidth} >{\raggedright\arraybackslash}p{0.3\textwidth} >{\raggedright\arraybackslash}p{0.22\textwidth} >{\raggedright\arraybackslash}p{0.2\textwidth}}
\toprule
Variant & Sparsity mechanism & Strength & Main concern \\
\midrule
Vanilla SAE & $L_1$ on activations & Simple and standard & Shrinkage bias \\
TopK SAE & Hard top-$K$ per token & Direct control, cleaner sparsity & Choosing good $K$ \\
FC\_SAE & Token allocation per feature & Better feature usage & Assignment complexity \\
\bottomrule
\end{tabular}
\end{frame}

\begin{frame}{5) Training Workflow}
\slideimg{SAE_workflow.png}
\begin{itemize}
  \item Sample layer activations $z$ from a target LLM layer
  \item Train SAE with reconstruction + sparsity objective
  \item Iterate with diagnostics: dead units, activation density, reconstruction quality
\end{itemize}
\end{frame}

\begin{frame}{5a) Training Health Check}
\slideimg{SAE_metrics.png}
\begin{itemize}
  \item Structural metrics: sparsity level, dead feature ratio, activation distribution
  \item Functional metrics: reconstruction fidelity and downstream steering utility
  \item Use these together to choose checkpoints, not only minimum loss
\end{itemize}
\end{frame}

\begin{frame}{5b) Full Training Curve Snapshot}
\slideimg{training_history.png}
\begin{itemize}
  \item Early training reduces reconstruction error quickly
  \item Later epochs refine sparsity organization and feature specialization
  \item Useful checkpoint zone: stable reconstruction + healthy utilization
\end{itemize}
\end{frame}

\begin{frame}{6) SAE vs Raw LLM Neuron View}
\slideimg{SAE_vs_LLM_comparison.png}
\begin{itemize}
  \item SAE features are typically cleaner than individual polysemantic neurons
  \item Better decomposition supports concept-level intervention and tracing
  \item Remaining overlap indicates interpretability is improved, not solved
\end{itemize}
\end{frame}

\begin{frame}{6a) Results -- Reconstruction/Interpretability Balance}
\slideimg{SAE_architecture2.png}
\begin{itemize}
  \item Too little sparsity: excellent reconstruction but weaker disentanglement
  \item Too much sparsity: cleaner features but information loss
  \item Practical target: lowest error that still yields stable semantic features
\end{itemize}
\end{frame}

\begin{frame}{7) Interpretation Protocol (Survey + Ng Notes)}
\begin{itemize}
  \item Collect top-activating tokens/sentences per feature
  \item Assign tentative semantic label from activation evidence
  \item Validate with counterexamples and cross-context checks
  \item Flag polysemantic or brittle features for retraining/tuning
\end{itemize}
\begin{block}{Ng-style intuition}
Interpretable hidden units correspond to sparse non-linear features of input space; visualization validates what each unit is detecting.
\end{block}
\end{frame}

\begin{frame}{7a) Common Failure Cases}
\begin{itemize}
  \item Dead features: never selected/activated under current data and hyperparameters
  \item Split concepts: one concept fragmented across many weak features
  \item Polysemantic leftovers: one feature still co-activates for unrelated contexts
  \item Dataset bias: semantic labels reflect corpus skew rather than true abstraction
\end{itemize}
\end{frame}

\begin{frame}{8) Inference UI -- Overview}
The inference UI (by ANTLR) has \textbf{three main tabs}:

\vspace{0.4em}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.24\textwidth} >{\raggedright\arraybackslash}p{0.68\textwidth}}
\toprule
Tab & Purpose \\
\midrule
\textbf{SAE Analysis} & Input text -> see top-K activated features per token \\
\textbf{Feature Map} & Search/label features across the full dataset \\
\textbf{Feature Trace} & Trace a specific feature ID over any input text \\
\bottomrule
\end{tabular}

\vspace{0.6em}
Built on top of GPT-2 SAE checkpoints (e.g. \texttt{best\_model\_fc\_sae\_m32\_layer10.pt})
\end{frame}

\begin{frame}{8a) SAE Analysis -- Token-level Feature Inspection}
\slideimg{ui_token_view.jpg}
\begin{itemize}
  \item User pastes any input text and clicks \textbf{Analyze}
  \item Hovering/tapping a token shows its \textbf{top-K activated features} on the right
  \item Here, token \texttt{language} -> top feature \texttt{\#3000: Language Concept} (activation \textbf{13.846})
  \item Remaining features (\#2071, \#3296, ...) show lower but non-trivial activations
\end{itemize}
\end{frame}

\begin{frame}{8b) Feature Map -- Dataset Activation Search}
\slideimg{ui_feature_map.jpg}
\begin{itemize}
  \item Given a \textbf{Feature ID}, scans the dataset (MLCommons/peoples\_speech) for activating sentences
  \item Feature \#6068 labelled \emph{"Emphasis or extent"} -- found 4 matches in 200 sentences
  \item Highlighted token per sentence shows \textbf{exactly which token triggered} the feature
  \item Supports \textbf{Bulk Label} mode to label many features at once via LLM
\end{itemize}
\end{frame}

\begin{frame}{8c) Feature Analysis -- LLM Auto-labeling}
\slideimg{ui_sae_analysis.jpg}
\begin{itemize}
  \item For any feature, the system sends top-activating examples to an LLM for \textbf{automatic labeling}
  \item Feature \#3425 -> labelled \emph{"Legal Court Context"} with confidence \textbf{HIGH}
  \item Top token: \texttt{court} -- consistent across all matched sentences
  \item Parameters: sentences to scan, examples sent to LLM, min activation threshold
\end{itemize}
\end{frame}

\begin{frame}{8d) Feature Analysis -- Activation Examples}
\slideimg{ui_feature_examples.jpg}
\begin{itemize}
  \item Matched sentences use \texttt{>>>token<<<} to mark the trigger token
  \item Each example includes an activation score (e.g. 15.10, 14.58)
  \item Supports manual verification of LLM-assigned labels
  \item Helps catch mislabeled or polysemantic features
\end{itemize}
\end{frame}

\begin{frame}{8e) Feature Trace -- Single-feature Token Heatmap}
\slideimg{ui_feature_trace.jpg}
\begin{itemize}
  \item Input text + Feature ID -> highlights \textbf{which tokens} activate that feature
  \item Feature \#2873 (\emph{"Numerical Quantity"}) fires on token \texttt{hundred} in the sentence
  \item Right panel: \textbf{Token Activation Map} with underline intensity as a visual cue
  \item Stats shown: active tokens, token count, max activation, threshold
\end{itemize}
\end{frame}

\begin{frame}{Closing}
\begin{itemize}
  \item Superposition motivates feature-level decomposition
  \item Sparsity makes decomposition practical and interpretable
  \item Modern SAE variants improve utilization and robustness over vanilla L1 baselines
  \item Reliable interpretation needs both good architecture and good evaluation protocol
\end{itemize}
\end{frame}

\begin{frame}{References}
\footnotesize
\begin{itemize}
  \item Ayonrinde et al. (2024). \emph{Sparse Autoencoders for LLM Interpretability: A Survey}.
  \item Ng, A. (UFLDL notes). \emph{Sparse Autoencoder}.
  \item Elhage et al. (2022). \emph{Toy Models of Superposition}.
\end{itemize}
\end{frame}

\begin{frame}[plain]
\centering
\Large Thank you.
\end{frame}

\end{document}
